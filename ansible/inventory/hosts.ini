; NexGenAds Home Server inventory
;
; Usage:
;   ansible-playbook -i inventory/hosts.ini playbooks/server.yml
;
; ansible_host / ansible_user are defined once in group_vars/all.yml so
; they only need to be changed in a single place.

[home_server]
home
